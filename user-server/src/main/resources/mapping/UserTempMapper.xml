<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ihq.capitalpool.userserver.Mapper.UserTempMapper">
    <insert id="insert"  parameterType="com.ihq.capitalpool.userserver.Entity.UserTemp" useGeneratedKeys="true" keyProperty="id">
        insert into user_auth.user_temp(username, name, email, phone, password, active_code) values
        (#{email}, #{name}, #{email}, #{phone}, #{password}, #{activeCode});
    </insert>

</mapper>